/**
 * A recurring rule used to derive future zone-offset transitions.
 *
 * The current runtime does not expose concrete recurring rule instances, but
 * the type remains part of the public compatibility surface.
 */
export interface ZoneOffsetTransitionRule {}
