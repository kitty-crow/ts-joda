name: Export current refactor

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git archive --format=tar.gz --output=ts-joda-current.tar.gz HEAD
      - uses: actions/upload-artifact@v4
        with:
          name: ts-joda-current
          path: ts-joda-current.tar.gz
          if-no-files-found: error
          retention-days: 1
